const missingNumber = require('./Find-the-Missing-Number');

describe('', () => {
  it('', () => {
    expect(missingNumber([2, 7, 11, 15])).toEqual(0);
   
    expect(missingNumber([0,2])).toEqual(1);
    
  });

});