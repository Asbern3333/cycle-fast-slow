const removeDuplicates = require('./Remove-Duplicates');

describe('', () => {
  it('', () => {
    expect(removeDuplicates([2, 7, 11, 15,2])).toEqual([2, 7, 11, 15]);
   
    expect(missingNumber([0,1,1,2])).toEqual([0,1,2]);
    
  });

});